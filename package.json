{
  "name": "visage-exercise",
  "version": "1.0.0",
  "scripts": {
    "start": "cd backend && node src/server.js",
    "build": "cd frontend && npm i && npm run build"
  },
  "dependencies": {
  }
}
