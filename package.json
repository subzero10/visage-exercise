{
  "name": "visage-exercise",
  "version": "1.0.0",
  "scripts": {
    "start": "cd backend && node src/server.js",
    "build": "cd backend && npm ci && cd ../frontend && NPM_CONFIG_PRODUCTION=false npm ci && npm run build"
  },
  "dependencies": {
  }
}
