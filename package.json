{
  "name": "visage-exercise",
  "version": "1.0.0",
  "scripts": {
    "start": "cd backend && node src/server.js",
    "build-backend": "cd backend && npm ci",
    "build-frontend": "cd frontend && NPM_CONFIG_PRODUCTION=false npm ci && npm run build",
    "build": "npm run build-frontend"
  },
  "dependencies": {
  }
}
